name: wave
description: Gets your duties sorted. Gets your money right.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^4.4.0
  firebase_auth: ^0.15.5+3
  cloud_firestore: ^0.13.4+2
  form_field_validator: ^1.0.1
  flutter_icons: ^1.0.0+1
  provider: ^4.0.4
  sliding_up_panel: ^1.0.1
  flutter_datetime_picker: ^1.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
#  assets:
#    - images/image.png
