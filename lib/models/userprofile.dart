class UserProfile {
  final String fullName;
  final String firstName;
  final String lastName;
  final String address;
  final String bio;
  final String accountType;

  UserProfile({this.fullName, this.firstName,
      this.lastName, this.address, this.bio, this.accountType});
}
